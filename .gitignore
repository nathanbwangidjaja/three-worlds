# dependencies & builds
node_modules/
dist/
.vite/

# secrets — API keys live in client/.env (copy from client/.env.example)
.env
.env.*
!.env.example

# scraped reference imagery (large, regenerate with tools/scrape-refs.js)
refs/

# local tooling & junk
.claude/settings.local.json
.DS_Store
*.log
# NOTE: music/*.mp3 is intentionally TRACKED so full songs deploy with the site.
# (Repo is public — these are copyrighted tracks. That's an accepted tradeoff
# for this personal gift. Make the repo private if that matters to you.)
